# JWT Configuration
# IMPORTANTE: Altere a chave secreta em produção!
# Use um valor forte e único, por exemplo: openssl rand -base64 32

JWT_SECRET_KEY=your-secret-key-change-in-production-use-strong-random-value
JWT_EXPIRATION_TIME=3600
